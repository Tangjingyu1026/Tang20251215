# **应用QT 13-21天**

### **功能分析（1天）**



### **开发环境搭建配置交叉编译工具链 （1-2天）**

Windows：安装 QtCreator，配置编译器（MinGW 或 MSVC），验证 serialport 示例

Ubuntu ARM：配置交叉编译工具链

### **通信底层类编写（1天）**

SUM 累加校验和算法，报文的拼接与解析

### **页面搭建（1-2天）**

使用 Qt Designer 设计主窗口画出控制界面：

串口设置区（端口下拉、打开/关闭按钮、参数显示）

开关量控制区（Y0~Y7，Y10-Y17，每个点两个按钮或一个切换按钮）

状态显示区（读取Y状态输出报文、读取X状态输入报文）

日志显示区（用于调试）

语言切换下拉框

布局调整，在不同分辨率下和DPI下界面显示正常





使用 Qt Designer 画出控制界面。

注意使用布局管理器（Layouts），确保在不同分辨率和系统缩放（DPI）下界面不会变形。

### **多语言编写（1天）**

使用 lupdate 生成 .ts 翻译文件（中英文）

用 Qt Linguist 完成翻译

实现动态语言切换：更改应用程序翻译器并重绘界面

### 业务逻辑整合 **（3-4天）**

底层的串口数据与前端的UI 控件通过 Qt 的信号与槽机制绑定

实现定时器，定时向单片机发送轮询指令读取状态。

### **编写应用Windows Ubuntu 适配（2-3）**

串口号的差异

处理 Linux 下串口访问的权限问题

 高 DPI 设置

### **程序打包部署（1天）：**

Windows：使用 windeployqt 提取依赖的 dll 动态库，制作成无需安装 Qt 也能运行的绿色文件夹，或者使用 Inno Setup 打包成 .exe 安装包。

Ubuntu：打包依赖库，编写运行脚本（.sh）。

### **编写应用测试 Windows 7,10,11 兼容性稳定性（2-3）**

**Ubuntu 兼容性稳定性（1-2天）：**

将编译好的 ARM 程序放进目标板卡运行，测试硬件串口通信是否畅通。